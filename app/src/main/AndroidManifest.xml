<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".NotiLoggerApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotiLogger"
        tools:targetApi="31"
        android:icon="@drawable/ic_app_logo"
        android:roundIcon="@drawable/ic_app_logo">

        <activity
            android:name=".LockActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />
        
        <activity android:name=".DetailsActivity" android:exported="false" />
	<activity
	    android:name=".FilterActivity"
	    android:exported="false" />

	<activity
	    android:name=".SecurityActivity"
	    android:exported="false" />
	    
	<activity
	    android:name=".KeywordFilterActivity"
	    android:exported="false" />
	    
	<activity
	    android:name=".TelegramBotsActivity"
	    android:exported="false" />

	<activity
	    android:name=".BotDetailsActivity"
	    android:exported="false" />

	<activity
	    android:name=".ChatDetailsActivity"
	    android:exported="false" />

	<service android:name=".NotificationService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
